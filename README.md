# Bash-scripts
Scripts for the Linux Command Line
## Solidtime CLI
uses the solidtime free API to use basic functionalities such as:
- Start new timer:  ```solid start```
- Stop (discard/save) the timer ```  ctrl + C ```
-  View the daily log and total time by Project  ``` solid log ``` 
